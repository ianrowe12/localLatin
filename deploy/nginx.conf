# LocalLatin root nginx location.
#
# Merge these location blocks into the active ai.csr.uky.edu HTTPS server block.
# The ai.csr.uky.edu hostname is owned by the LocalLatin team, so the app can
# live at the root URL.

location / {
    proxy_pass http://127.0.0.1:8080;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 120s;
    proxy_send_timeout 120s;
}
