# Plan of 2026-09-06: ARR October round 2

Source: meeting with Siddique and James (2026-09-06), Prof. Firey's two emails and
annotated PDF (2026-09), James Wong's Overleaf edits and comments (2026-09-03).
Tracking: epics #107 (benchmark v1 + TEI pipeline), #108 (paper revision),
#109 (experiments round 2), #110 (ops cleanup + data refresh).

## Decisions (Ian, 2026-09-06)

1. Apply Firey's two key corrections now and re-derive every paper number
   (labels move, texts do not; no re-embedding). Freeze the result as
   **benchmark v1** for the paper.
2. New CCL material flows to the webapp and a future v2 only, never into the
   paper's numbers before submission.
3. Vocabulary follows Firey: "labeled fragment (canon)" with "manuscript
   witnesses". The word "family" is never used for our directories. "Strict"
   is dropped.
4. The six-model cosine-gap figure becomes Figure 1; the density figure moves
   to the appendix.
5. Attribution metrics: keep rho_LOO, add standard principled metrics (AOPC,
   deletion/insertion AUC, Kendall tau, randomization check), and move the
   ERASER trio to the appendix with one plain sentence. Nothing is deleted.
6. Fine-tuning ceiling on LaTa only, GPU approved (~2 h).
7. PCA whitening stays a short discussion paragraph, no table.
8. Regular meeting slot with Firey: yes. Meeting with Noah (CCL developer):
   optional once the TEI converter exists.

## Correction to the record

Ian told the professor lexical baselines had been done. A repo and archive
search on 2026-09-06 found none (no BM25, Levenshtein, BLEU, ROUGE, TF-IDF or
rule-based retrieval baseline). They are built from scratch in #122.

## Data facts established 2026-09-06

- Firey's TEI export for BN2123 (`/u/irowerojas/BN2123.xml`, 508 units)
  converts to our plain-text layout by this rule: text = `msSource` rubric +
  `canonText` (inline editorial notes rendered), `scholSource` key stripped;
  directory = key. Our `BN2123.89r.1.txt` matches this rule exactly.
- Key corrections (fol. 89r): `BN2123.89r.5` Can.apost.48 -> 49 (train file);
  `BN2123.89r.6` Can.apost.49 -> 50 (test query).
- BN2123 has 140 labelled and 148 unlabelled units in our data; the export has
  508, so future exports carry new material.

## Prof. Firey's PDF annotations (acl_latex-Firey.pdf, pages 1-3)

| Highlighted | Her comment |
|---|---|
| "survives as families of manuscript copies, shaped by spelling variation, fragmentary copying, and editorial change" | "before the twelfth century survives in a plethora of compilations; the individual manuscripts of each compilation will have spelling variation and perhaps differences in the selection and arrangement of the elements." |
| consolidate | identify |
| link related | relate different witnesses |
| strict s[emantic] | meaningful |
| meaning-equivalent families | What does this mean? 840 sets with one or more matching pairs of fragments? One "family" = all witnesses to a single labeled fragment? Or same-topic fragments? Or new categories? |
| legal reasoning | legislation and judicial opinions |
| Manuscript copies | Just "Manuscripts" suffices |
| editorial history | transmission history |
| everyday | omit |
| consolidating | identifying |
| linking | analysing |
| 840 families of meaning-equivalent variants | Not clear. "840 sets of one or more matching pairs"? The other half of the dataset comprises unique fragments with no match yet? "Family" has a technical sense for textual scholars (a family of related manuscripts or compilations) and should only be used in that sense. |
| arbitrary | randomly selected? |
| strict semantic matching | What defines "strict"? Given the variation in handwritten replication we may not want something entirely strict. |
| one legal text | "one labeled fragment in multiple manuscript copies" is what you mean; "one legal text" could mean a sequence of 350 fragments. |

## James Wong's Overleaf comments (2026-09-03)

1. Figure 1: term mismatch between figure ("Dip Layer") and caption
   ("collapsed layer"); figure and caption must be self-contained.
2. Decide whether to include PCA whitening; a short explanation of why SIF and
   whitening underperform ABTT would strengthen the ABTT finding.
3. "non-T5 models" rather than "remaining models" (fig:gap caption).
4. "T5 encoders" rather than "headline models" (tab:layer_diagnostics_main).
5. Typo "Retrieval-MarK".
6. Consistency: "SIF-only" and "ABTT-only".
7. (Teams) Many of these need changes in the table/figure generators so they
   stick across regeneration.

## Order of work

Package 3 (ops, #126) first. Then Epics E and G in parallel with the text-only
paper issues (#114, #115, #117, #119). Generator-side paper issues (#116,
#118, #120) wait for #113 and #124. #121 is last, then the mirror sync and the
data refresh (#127).
