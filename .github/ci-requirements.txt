# Pinned CI-only tooling (test + lint). Kept out of web/requirements.txt because
# these are not runtime dependencies of the webapp. This file is part of the pip
# cache key in .github/workflows/ci.yml, so bumping a pin invalidates the cache.
httpx==0.28.1
pytest==9.1.1
ruff==0.16.4
# tests/test_lexical_baselines.py: scipy backs the sparse BM25 term-count matrix,
# scikit-learn the character n-gram TF-IDF vectoriser, rapidfuzz the Levenshtein
# matrix. All three are pure-wheel installs, unlike the torch/transformers stack
# in the root requirements.txt that this file deliberately avoids.
rapidfuzz==3.14.5
scikit-learn==1.7.2
scipy==1.15.3
